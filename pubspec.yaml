name: elz0mbi33
description: "Floating Spotify lyrics app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.5
  http: ^1.2.2
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.0
  window_manager: ^0.4.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
